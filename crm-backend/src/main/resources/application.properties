quarkus.http.port=8080
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=${CORS_ORIGINS:*}
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with

quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/crm-maferg}
quarkus.datasource.username=${DB_USER:postgres}
quarkus.datasource.password=${DB_PASSWORD:timbersaw}

# URL del Frontend (utilizada para generar el QR)
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# Configuración de Brevo Email API (HTTPS)
app.email.api-key=${EMAIL_API_KEY:}
app.email.sender-name=MAFER-G
app.email.sender-email=maferggg12@gmail.com

